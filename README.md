Configuration preset for TypeScript.

## Usage

**Install**:

```bash
$ yarn add --dev typescript-preset
```

**Edit `tsconfig.json`**:

```tsconfig.json
{
  "extends": "typescript-preset",
  "compilerOptions": {
    // Override certain options to fit your needs.
    "strict": false,
  }
}
```
